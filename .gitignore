CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps
CMakeUserPresets.json
/.vs
/out/build/x64-debug
build*/
__pycache__
*.pyc

# pytest drops this at the root the moment `just test` runs the scripts_tests suite.
.pytest_cache/

# Local, developer-specific editor config (like .env); seeded from config.example.json
apps/editor/config.json

# `bcp-feature` keeps each in-progress feature's slice plan here. Working state for one clone.
.claude/features/

# Per-developer Claude Code settings, layered over the committed .claude/settings.json.
.claude/settings.local.json

# `just install` stages the CLI tools here; this is the dir you put on PATH.
dist/

TODO.md

# A .bvat is wholly derived from the .bmesh/.bskel/.banim it stamps -- baked on demand, never
# committed. An accidental add would land tens of MB as a plain git blob.
*.bvat

# Finder drops these all over a macOS checkout; one `git add -A` and they land.
.DS_Store
