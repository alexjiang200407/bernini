# Features
1. Resize Render Target when user finishes the resizing when resizing the window 
2. In gamelib create a AssetManager for storing and caching Assets and handling refcounts etc.
3. Maybe add a material swapping system where the user can override the meshes material
4. Fix the scripts so that it uses a config instead of command line arguments. Also create a script for configuring it. 

# Bugs
1. Sometimes, when exiting, refuses to exist
2. Scrolling in the material node editor will reduce frame rate for the material preview.