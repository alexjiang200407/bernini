# Features
1. Delete unused baked textures
2. Resize Render Target when user finishes the resizing when resizing the window 
3. In gamelib create a AssetManager for storing and caching Assets and handling refcounts etc.
4. Maybe add a material swapping system where the user can override the meshes material
5. Fix the scripts so that it uses a config instead of command line arguments. Also create a script for configuring it. 

# Bugs
1. Sometimes, when exiting, refuses to exist
2. Scrolling in the material node editor will reduce frame rate for the material preview.