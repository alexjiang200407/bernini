# Test code is engine code, so its fixtures are PascalCase like the rest of the engine. Only
# assetlib's own published functions are camelBack -- see ../.clang-tidy -- and a helper that builds a
# cube to feed one is not part of that vocabulary.
InheritParentConfig: true

CheckOptions:
  readability-identifier-naming.FunctionCase: CamelCase
