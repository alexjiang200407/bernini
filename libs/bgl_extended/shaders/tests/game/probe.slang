// The file a source-loaded module of the same dotted name shadows: what CSSourceProbe reads when no
// module named game.probe has been loaded from text. A subdirectory, because that is the shape the
// engine's slot binding has -- game/slot0.slang under import game.slot0.
public static const uint kProbeValue = 1u;
